[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "roof-seg"
version = "0.1.0"
description = "Roof segmentation on satellite imagery (dida take-home task)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["roof_seg*"]
